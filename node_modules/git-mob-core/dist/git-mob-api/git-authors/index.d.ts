export function gitAuthors(readFilePromise: any, writeFilePromise: any, overwriteFilePromise: any): {
    read: () => Promise<any>;
    write: (authorJson: any) => Promise<any>;
    overwrite: (authorJson: any) => Promise<any>;
    fileExists: () => boolean;
    coAuthors(authorInitials: any, authorJson: any): any;
    author(authorInitials: any, authorJson: any): any;
    coAuthorsInitials(authorJson: any, currentCoAuthors: any): never[];
    toList(authors: any): Author[];
};
export function pathToCoAuthors(): string;
import { Author } from "../author";
